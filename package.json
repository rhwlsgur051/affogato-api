{
  "name": "server-graphql",
  "version": "1.0.0",
  "description": "graphql+express+sequelize",
  "main": "server.ts",
  "author": "",
  "scripts": {
    "start": "cross-env NODE_ENV=local node ./dist/src/server.js",
    "dev": "cross-env NODE_ENV=local ts-node-dev --respawn --transpile-only src/server.ts",
    "watch": "cross-env NODE_ENV=local nodemon --config nodemon.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc":"tsc -p tsconfig.build.json",
    "build":"rm -rf dist && npm run tsc"
  },
  "keywords": [],
  "license": "ISC",
  "dependencies": {
    "apollo-server": "^2.25.0",
    "apollo-server-express": "^2.25.0",
    "bcrypt": "^5.0.1",
    "cross-env": "^7.0.3",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "graphql": "^15.5.0",
    "jsonwebtoken": "^8.5.1",
    "mysql2": "^2.2.5",
    "sequelize": "^6.6.2"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.0",
    "@types/jsonwebtoken": "^8.5.1",
    "@types/node": "^15.6.1",
    "nodemon": "^2.0.7",
    "ts-node": "^10.0.0",
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.3.2"
  }
}
